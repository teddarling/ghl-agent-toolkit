# Secrets — never commit
.env
.env.local

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
dist/
build/
*.egg-info/

# Agent run artifacts
llm-trace.jsonl
audit.log.jsonl
proposals.jsonl

# Per-business tagging rules (commit the .example, not your real rules)
tagging-rules.yaml

# Node (web/ dashboard, Phase 6)
node_modules/

# OS / editor
.DS_Store

# Per-business reply guidelines (commit the .example, not your real file)
reply-guidelines.yaml
